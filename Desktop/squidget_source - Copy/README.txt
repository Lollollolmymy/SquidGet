squidget — source code
=======================
Build on any platform:

  macOS / Linux:   ./run.sh     (auto-installs deps, compiles, launches)
  Windows:         squidget.bat (downloads portable GCC, compiles, launches)
  Manual:          make

Dependencies: gcc/clang + libcurl
